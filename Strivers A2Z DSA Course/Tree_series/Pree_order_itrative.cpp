  /*
  // an vector
    vector<int>ans;
     if(root == NULL) return  ans;
    //stack 
     stack<TreeNode*>st;

    // insedrting first elemtn;
     st.push(root);
     while(!st.empty()){
        //  top of the stack i.e first element
      root = st.top();
      // removel of thhe forst element 
      st.pop();
      ans.push_back(root->data);

      if(root->right !=NULL){
        st.push(root->right);
      }
      if(root->left !=NULL){
      st.push(root->left);
      }
     }
     return ans;
     */