/*
int majorityElement(vector<int>& nums) {
        sort(nums.begin(),nums.end());

        int n = nums.size();
          n = n/2;
        return nums[n];
    }
*/

// The majority element is the element that appears more than ⌊n / 2⌋ times. Remember this thing
