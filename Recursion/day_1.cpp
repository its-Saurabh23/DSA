#include<bits/stdc++.h>
using namespace std;

// when a function call itself is called recursoin.

/*int fact(int n){

    // base case;
    if(n == 0){
        return 1;
    }
     // Recurance Relatoin
    //  int chotti = fact(n-1)
    //  int  badi  = n *choti; 
    // int ans =   n * fact (n-1);
    // return ans;
    return  n * fact (n-1);
}

int power(int n ){

    // base case
    if(n ==0){ 
        return 1;
    }
// Recurance Ralation.
// int chotti = power(n-1);
// int badiProb = 2 * chotti;


// return  badiProb;
return 2 * power(n-1);
}

void count(int n){
    // base case
    if(n == 0){
        return;
    }
    // Recurance ralaton
    cout<<n<<endl;
     count(n-1);

}*/
int  main(){
int n ;
cin>>n;
/*  int ans = fact(n);
//  cout<<"Factorial "<<ans<<endl;
int ans =  power(n);
cout<< "Ans "<<ans<<endl;

cout<<endl;
 count (n);
 */
    return 0;
}