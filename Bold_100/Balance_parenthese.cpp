#include<bits/stdc++.h>
using namespace std;

bool BalanceParenthese(string &str){
    stack<string>st;
      for(char ch : st){
         if(ch == "(" || ch == "[" || ch =="{"){
st.push(ch);
}elseif ()
      }
    } 
}

int  mian(){
    char str ="({[[]]})";
    BalanceParenthese(str);
    return 0;
}

// Parentheses 