#include <bits/stdc++.h>
using namespace std;

/*
int Queue(Queue<int>q){

     stack<int> s;

    // pop from queue
    while(!q.empty()){
        int element = q.front();
        q.pop();
        s.push(element);
    }

    while(!s.empty()){
        int element = s.top();
        s.pop();
        q.push(element);
    }
    return q;
}
*/
int main()
{
    return 0;
} 